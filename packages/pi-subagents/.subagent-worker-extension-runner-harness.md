Implemented extension runner harness refactor.

Changed files:
- `packages/pi-subagents/test/integration/intercom-live.test.ts`
  - Replaced custom fake Pi registration/lifecycle harness with upstream `@mariozechner/pi-coding-agent` `discoverAndLoadExtensions`, `createEventBus`, and `ExtensionRunner`.
  - Harness now loads actual extension entrypoint through Pi loader.
  - Harness now emits `session_start` / `session_shutdown` through `ExtensionRunner`.
  - Tool lookup now uses `runner.getToolDefinition()`.
  - Context now comes from `runner.createContext()`.
  - Runtime/core behaviors still mocked: model, session manager, UI, active tools, send/append capture.
  - Added runner-backed assertions for normal `intercom` registration and child `contact_supervisor` registration.
- `packages/pi-subagents/plan-extension-runner-harness.md`
  - Checked all tasks complete.
- `packages/pi-subagents/.subagent-worker-extension-runner-harness.md`
  - This result report.

Validation:
- Targeted intercom integration test passed:
  - `cd packages/pi-subagents && node --experimental-transform-types --test test/integration/intercom-live.test.ts`
  - 6 pass, 0 fail.
- Full package test passed before final comment-only edit:
  - `cd packages/pi-subagents && npm test`
  - 67 pass, 0 fail.
- Targeted intercom integration test re-ran after final comment edit and passed again:
  - 6 pass, 0 fail.

Diff notes:
- `createHarness()` is now async; call sites updated with `await createHarness(...)`.
- Direct `registerSubagentExtension` import removed.
- Old fake `pi.on`, `registerTool`, lifecycle handler map, and manual lifecycle dispatch removed.
- Kept helper inline in `intercom-live.test.ts`; extraction not needed yet.

Open risks/questions:
- Full `npm test` was not re-run after final comment-only edit, but targeted affected test was re-run and passed.
- Existing `package.json` still only runs `test/**/*.test.ts`; colocated `src/**/*.test.ts` remain outside this task.

Recommended next step:
- Optional: run full `npm test` once more in review/CI.
