{
  "name": "pi-extensions",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "pi": {
    "extensions": [
      "./packages/pi-tree-map/index.ts",
      "./packages/pi-answer/index.ts",
      "./packages/pi-auto-label/index.ts"
    ]
  },
  "scripts": {
    "check": "npm run -ws --if-present check",
    "build": "npm run -ws --if-present build"
  }
}
